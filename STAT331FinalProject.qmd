---
title: "STAT 331 Final Project"
author: "Noel Lopez, Patrick George, Riley Svensson, Ningjing Hua"
format: 
  html:
    self-contained: true
    code-tools: true
    toc: true
    number-sections: true
    theme: 
      dark: superman
    code-fold: true
editor: source
execute: 
  error: true
  echo: true
  message: false
  warning: false
---

```{r setup}
library(tidyverse)
energy <- readxl::read_xlsx(here::here("renewable_energy.xlsx"))
co2 <- readxl::read_xlsx(here::here("co2.xlsx"))

```

```{r clean data}

if(`ï»¿country` %in% colnames(energy))
{
  energy <- energy |> rename(country = `ï»¿country`)
}

if(`ï»¿country` %in% colnames(co2))
{
  co2 <- co2 |> rename(country = `ï»¿country`)
}

energy_clean <- energy |>
  pivot_longer(cols = !country,
               names_to = "year",
               values_to = "Percent_Consumption")

co2_clean <- co2 |> 
    select(country,`1989`:`2017`) |>
    pivot_longer(cols = !country,
               names_to = "year",
               values_to = "1000_metric_tonnes")

co2_clean <- co2_clean |>
   mutate(thousand_metric_tons = case_when(
    str_detect(`1000_metric_tonnes`, "k") ~ 
      as.integer(str_replace(`1000_metric_tonnes`, "k", "")) * 1000,
    str_detect(`1000_metric_tonnes` , "M") ~ 
      as.integer(str_replace(`1000_metric_tonnes` , "M", '')) * 1000000,
    TRUE ~ as.numeric(`1000_metric_tonnes`))) |>
  select(country, year, thousand_metric_tons)



master <- left_join(co2_clean, energy_clean) |> 
  drop_na()
```
# Detailed Data and Variable Description

# Hypothesized Relationship

# Data Cleaning Process and Decisions